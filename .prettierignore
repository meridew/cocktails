node_modules
build
.svelte-kit
package-lock.json

# neo.css is a VERBATIM copy of the original hand-made design (the pre-rewrite
# styles.css). Reformatting it destroys the ability to diff the two, which is the
# only check that the port hasn't drifted — Prettier reflowed it once already,
# 767 lines to 2007, when the file moved and lost this exemption.
src/lib/neo.css
