# Archive

Completed work, kept for the reasoning rather than the instructions. **Nothing in
here describes the current stack** — several were written against the two-app
monorepo that no longer exists, and following their steps would be wrong.

For how things work today, read [`../handoff.md`](../handoff.md).

|                     | what it was                                | why it's here                                                                                      |
| ------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------- |
| `PLAN.md`           | the original modernisation roadmap         | every phase shipped                                                                                |
| `QUALITY-PLAN.md`   | the audit → tests → refactor programme     | shipped; the engineering standards in §1 still apply                                               |
| `APP-READINESS.md`  | getting to "ready to build the native app" | the web side is done; the native build is deferred                                                 |
| `CUTOVER.md`        | public HTTPS via Cloudflare Tunnel         | done — though it describes the Caddy path, which is gone                                           |
| `MOBILE.md`         | the Capacitor build & publish runbook      | never started; paths are from the old layout, but the store requirements and icon sizes still hold |
| `SVELTEKIT-PLAN.md` | the migration to one SvelteKit app         | done; §12 records what the "no users" assumption bought and when to undo each part                 |
