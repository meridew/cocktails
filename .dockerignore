**/node_modules
.git
.svelte-kit
data
**/data
*.sqlite
*.sqlite-*
.env
**/.env
*.local
tests
docs
apps
packages
static
src

# ...except the built server, which is produced off-box and copied in. The NAS
# runs out of memory compiling it (see Dockerfile), so the artifact ships in
# rather than being made here.
!build
!build/**
