/* ============================================================
   CONFIG — set this to your NAS order API endpoint.
   Until it points at a real https:// URL (no "CHANGE-ME"):
     • the order form falls back to Formspree email, and
     • bartender mode stays on-device (this browser only).

   Example:
     window.COCKTAIL_API = "https://yourname.synology.me/cocktail-api.php";
   ============================================================ */
window.COCKTAIL_API = "https://meridew.asuscomm.com:9443/cocktail-api.php";
